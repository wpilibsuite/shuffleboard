build_script:
  - gradlew.bat shadowJar --no-daemon --stacktrace

test_script:
  - gradlew.bat check --no-daemon --stacktrace -PlogTests -Pheadless

clone_depth: 1

cache:
  - '%USERPROFILE%\.gradle'
